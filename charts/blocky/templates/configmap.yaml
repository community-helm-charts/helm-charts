apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ printf "%s-config" (include "blocky.fullname" .) | trunc 63 | trimSuffix "-" }}
  namespace: {{ include "common.names.namespace" . | quote }}
  labels: {{- include "common.labels.standard" (dict "customLabels" .Values.commonLabels "context" $) | nindent 4 }}
    app.kubernetes.io/component: dns-proxy
  {{- if .Values.commonAnnotations }}
  annotations: {{- include "common.tplvalues.render" (dict "value" .Values.commonAnnotations "context" $) | nindent 4 }}
  {{- end }}
data:
  00-base.yml: |
    {{- include "blocky.renderBaseConfig" . | nindent 4 }}
  {{- if .Values.blocky.upstreams }}
  10-upstreams.yml: |
    {{- include "blocky.renderUpstreamsConfig" . | nindent 4 }}
  {{- end }}
