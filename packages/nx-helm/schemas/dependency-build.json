{
  "$schema": "https://json-schema.org/schema",
  "version": 2,
  "title": "Nx Helm Dependency Build Executor",
  "type": "object",
  "properties": {
    "chartRoot": {
      "type": "string",
      "description": "Workspace-relative chart directory."
    },
    "distDir": {
      "type": "string",
      "description": "Workspace-relative directory containing packaged dependency charts.",
      "default": "dist"
    },
    "internalRepository": {
      "type": "string",
      "description": "Helm repository URL used to identify dependencies that are local workspace charts."
    }
  },
  "required": [
    "chartRoot"
  ],
  "additionalProperties": false
}
