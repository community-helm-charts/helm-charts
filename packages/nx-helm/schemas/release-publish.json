{
  "$schema": "https://json-schema.org/schema",
  "version": 2,
  "title": "Nx Helm Release Publish Executor",
  "type": "object",
  "properties": {
    "chartName": {
      "type": "string",
      "description": "Helm chart name. Defaults to the name declared in Chart.yaml."
    },
    "repository": {
      "type": "string",
      "description": "Default Helm chart repository to publish to."
    },
    "chartRoot": {
      "type": "string",
      "description": "Workspace-relative chart directory."
    },
    "distDir": {
      "type": "string",
      "description": "Workspace-relative directory containing packaged charts.",
      "default": "dist"
    },
    "dryRun": {
      "type": "boolean",
      "description": "Print the helm push command without publishing.",
      "default": false
    },
    "registry": {
      "type": "string",
      "description": "Nx release publish registry override. For Helm this is treated as the chart repository URL."
    },
    "tag": {
      "type": "string",
      "description": "Accepted for Nx release publish compatibility; Helm chart versions are encoded in the chart package."
    },
    "access": {
      "type": "string",
      "description": "Accepted for Nx release publish compatibility and ignored for Helm charts."
    },
    "otp": {
      "type": "number",
      "description": "Accepted for Nx release publish compatibility and ignored for Helm charts."
    },
    "firstRelease": {
      "type": "boolean",
      "description": "Accepted for Nx release publish compatibility and ignored for Helm charts."
    },
    "nxReleaseVersionData": {
      "type": "object",
      "description": "Nx release version metadata passed through by nx release publish."
    }
  },
  "required": [
    "chartRoot"
  ],
  "additionalProperties": false
}
